

scrollPathIntoView()Experimental
    experimental so left out for now

drawFocusIfNeeded()
    should never need this functionality
    google and experiment with it at a later date

getLineDash()
    can see no scenario where this is needed, plus can be reached through public ctx object